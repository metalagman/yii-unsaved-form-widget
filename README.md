Unsaved form widget for Yii
=======================
### Usage example ###
```php
$this->widget('lagman.vendor.yii-unsaved-form-widget.UnsavedFormWidget', [
    'message'=>'There are some unsaved data.',
    'form'=>$form,
]);
```